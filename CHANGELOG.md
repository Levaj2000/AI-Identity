# Changelog

All notable changes to AI Identity are documented in this file.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

### Added
- **Evidence Anchor checkpoint history is now public — with an off-infrastructure mirror.** Until now a signed Merkle checkpoint left our infrastructure only inside exported Case File bundles, so the "someone else already holds the checkpoint" property depended on bundle distribution. Two additions make it true by default: (1) an unauthenticated feed, `GET /evidence-anchor/checkpoints` (paginated oldest-first — append-only history makes ascending pages stable — filterable by `org_id`/`since`/`until`) plus a split-view spot check, `GET /evidence-anchor/checkpoints/{merkle_root}`, which answers "is the checkpoint my bundle verifies against part of the public record?" — a 404 on a root that verifies offline is the tamper signal, and the 404 body says so. Entries serve the exact DSSE envelope a bundle ships (byte-comparable), never the raw batch internals (`leaves`/`audit_log_ids`); same public-by-design posture as the JWKS, CDN-cacheable, with two new indexes (`signed_at`, `merkle_root`) so the unauthenticated paths can't trigger sequential scans. (2) A GitHub Actions mirror (every 6h + manual dispatch, no secrets) snapshots the full history onto the dedicated `evidence-anchor-mirror` branch of this public repo via stdlib-only `scripts/evidence_anchor_mirror.py`, which **enforces append-only before writing**: a rewritten batch slot, a vanished root, or a mutated envelope refuses the snapshot and fails the run loudly — the existing branch is the evidence. The Case File bundle README now tells recipients how to cross-check their bundle's root against the public record, and the architecture page's two Evidence Anchor blurbs state the public-feed + mirror claim. Detection story, honest scope (per-org trees ≠ one CT log; a same-org mirror is a raised bar, not full independence), and the deliberate metadata trade-off (opaque org UUIDs + batch volumes become public) are documented in `docs/forensics/evidence-anchor-public-feed.md`. **Run the first mirror snapshot via workflow dispatch after deploy.**

### Changed
- **Licensing made explicit: proprietary core, MIT verification surface.** The repo is public but carried no root LICENSE (the README badge linked to a nonexistent file; GitHub showed "license: null"), and the offline verifier — which ships to third-party auditors inside every Case File bundle and is pitched as "read the verifier, it IS the spec" — carried no license grant at all. Now: a root `LICENSE` states the split (platform proprietary; relying-party surfaces permissive), `cli/` is MIT (directory LICENSE + SPDX headers that travel with the embedded script into bundles), and the README License section + badge document which directory carries which license. Principle on record: trusting AI Identity evidence must never require trusting AI Identity. Bundles embed the verifier at export time, so new exports ship the MIT-headed script.
- **Marketing site: "tamper-proof" corrected to "tamper-evident" everywhere.** The product's guarantee is tamper-*evidence* (HMAC-SHA256 chained audit log — alterations are detectable, not impossible), which `/security` and `docs/forensics/trust-model.md` already stated correctly, but 36 occurrences of "tamper-proof" survived across 18 files in `landing-page/src` — the forensics free-tier CTA, site-wide meta descriptions and JSON-LD (`layout.tsx`, `schemas.ts`), all six `/vs/` competitor comparison tables, the healthcare (HIPAA/HITECH) and finance (SOC 2) compliance tables, pricing, changelog, the EU AI Act checklist, and five blog posts. All now read "tamper-evident"; surrounding copy unchanged. Extends the #378 single-page fix site-wide. Marketing site only — no product impact. (#389)
- **Marketing site: StandardsBadge copy flipped from "Contributing to" to "Merged into OCSF 1.9."** With the OCSF attestation work (PR #1661 and its companion PRs) merged on 2026-07-17, the badge's hedged "contributing to" framing is retired — the three linked OCSF PRs are now presented as merged into OCSF 1.9, matching the forensics-pipeline copy flip in #387. Marketing site only — no product impact. (#388)
- **Marketing site: legal industry page added, duplicate finance pages consolidated, Solutions nav grouped.** New `/industries/legal` page (ABA Model Rules 1.1/1.6/5.3, FRCP 26/34 + 37(e), court AI standing orders, EU AI Act Annex III) covering privileged-data scoping, tamper-evident audit chains, Case File export for e-discovery workflows, and attorney-in-the-loop gates — copy deliberately avoids "court-ready"/"admissible" pending attorney review. `/use-cases/financial-compliance` (a near-duplicate of `/industries/finance` — same hero and pitch, different regulator list) is merged into the industry page: SEC/FINRA/MiFID II now appear in its hero, meta description, regulation cards, and compliance-mapping table, and the old URL 308-redirects there to preserve backlinks and indexed pages (near-duplicate content was a suspected contributor to the 8-indexed/21-not-indexed GSC problem). The Solutions dropdown gains section labels (Use cases / Industries / Tools / Compare) in desktop and mobile nav. Marketing site only — no product impact. (#384)
- **Bundled verifier: `attestation` accepts the raw API response, not just the bare envelope (CLI 1.3.0).** The command's help text pointed at `GET /api/v1/sessions/{id}/attestation` — but that endpoint returns an `AttestationResponse` wrapper (`{id, org_id, ..., envelope: {...}}`), and feeding it the saved response failed with `Unexpected payloadType: None`; only the hand-extracted `envelope` field verified. The CLI now detects the wrapper (no top-level `payloadType`, DSSE-shaped `envelope` field), unwraps it automatically, and says so in both human and `--json` output (`details.unwrapped_envelope`); bare envelopes verify exactly as before. Help text, README, and REFERENCE updated to name both accepted shapes. **Bundles embed the verifier at export time, so already-downloaded bundles keep requiring the bare envelope; new exports pick up the fix.**
- **Bundled verifier: `inclusion-proof` no longer tracebacks on stdlib-only Pythons (CLI 1.2.1).** The `attestation` command already caught a missing `cryptography` package and printed an install hint, but the `inclusion-proof` path imported it bare — a Case File recipient on a machine without the package (e.g. macOS system `python3`, reproduced 2026-07-10) got a raw `ModuleNotFoundError` traceback mid-verification instead of guidance. Both public-key commands now share one guard (`_require_cryptography`) that runs before any crypto import and exits 2 with the `pip install cryptography` hint; `report`/`chain` stay stdlib-only and unaffected. The bundle README also stops claiming the verifier has "no dependencies" outright — it now says the chain/report checks are dependency-free and flags the one package the optional `inclusion-proof` check needs (CLI docs updated to match). **Bundles embed the verifier at export time, so already-downloaded bundles keep the old traceback; new exports pick up the fix.**
- **`www.ai-identity.co` now serves the public JWKS, so pre-#379 bundles keep verifying.** Case File bundles exported before the #379 README fix instruct recipients to `curl -o jwks.json https://www.ai-identity.co/.well-known/ai-identity-public-keys.json` — a 404 until now. The landing page (Next.js/Vercel) gains a **rewrite** (not a redirect: the bundle curl command has no `-L`, so a redirect would silently save the wrong body) that proxies the path to the live key set on `api.ai-identity.co`, preserving the `application/jwk-set+json` content type. Verified end-to-end: a previously shared bundle's own `inclusion-proof` command passes against a JWKS fetched through the rewrite. Belt-and-braces for already-distributed bundles; new exports point at the API host directly.
- **Case File bundle's JWKS curl command now points at the live host.** The inclusion-proof instructions in every generated bundle README told recipients to `curl` the public key set from `www.ai-identity.co/.well-known/ai-identity-public-keys.json` — a 404 (the landing page doesn't serve it). The JWKS is served by the API at `api.ai-identity.co/.well-known/ai-identity-public-keys.json` (verified live), so the offline Evidence Anchor verify flow's curl step was a dead link. Fixed in the bundle README (`api/app/routers/audit.py`) and in the one other absolute wrong-host reference in the repo, the standard motor-info sales deck. **Bundles downloaded before this deploys still carry the old URL — regenerate demo bundles after merge.** (#379)
- **Public forensics-pipeline explainer updated to the shipped integrity story.** The interactive page at `/forensics-pipeline.html` (landing-page Vercel project) predated several shipped mechanisms; it now carries a new click-to-inspect "Three Layers of Integrity" section (write-time **dual** keyed HMAC chains — platform-wide `entry_hash`/`prev_hash` plus per-org `entry_hash_org`/`prev_hash_org`/`org_chain_seq` completeness proof → export-time per-event ECDSA-P256 signatures verifiable against the public JWKS on `api.ai-identity.co` → Evidence Anchor Merkle checkpoints, 15-min cadence, DSSE, offline/in-browser inclusion proofs), a small Mandate Service node (signed spend limits, enforce/settlement, `exceeded` state, lifecycle events into the same chain), and the org-chain columns in the schema section. Claims re-scoped honestly: chain verification attributed to the org key-holder (public verification = export signatures + inclusion proofs), "tamper-proof" → "tamper-evident", OCSF referenced only as "proposed to OCSF (PR #1661, in review)", and footer fixed (`ai-identity.io` → `www.ai-identity.co`, stale date, "Confidential" label dropped from a public URL). Marketing page only — no product impact. (#378)
- _Docs:_ CoSAI WS4 OCSF issues working draft advanced to iteration 2 — applied IBM reviewer feedback (CMF-verified redline: `tool.primitive` discriminator, one correlation model pinning `correlation_uid` = run, spawn/delegation/derivation lineages separated, Issue 5 reframed as a both-sides co-design gap) and resolved all open schema verifications against upstream `ocsf/ocsf-schema` main and the live delegation PR #1665. Also hardened `.gitignore` so private collaboration and internal prep materials can't be committed to this public repo. Internal docs only — no product impact.
- **OCSF export now emits the final #1661 attestation shape, with per-event ECDSA signatures.** The `format=ocsf` Case File export was on an early draft of the #1661 `attestation` object; the shape that landed during schema review (at `fa4003ad`) is now what production emits. Concretely: the `attestation` object rides the **`record_integrity` profile** (declared in `metadata.profiles` whenever the object is present); `entry_hash`/`prev_entry_hash` became **fingerprint objects** with `algorithm_id` 99/Other + `algorithm: "HMAC-SHA-256"` (the chain hash is *keyed* HMAC — claiming plain SHA-256 would misstate the construction); `attestation.uid` carries the audit row id; and the schema-required **`signatures` array** is real — the export path signs each event ECDSA-P256-SHA256 over `bytes.fromhex(entry_hash)` with the KMS-backed forensic signer, the same message convention as Evidence Anchor's Merkle leaves, so the one published JWKS key verifies both. Signature bytes + key id ride `unmapped` (`signature_b64`, `signature_key_id`) since OCSF's `digital_signature` has no field for them; KMS signing is parallelized, and a dev setup with no signer degrades to an unsigned (structurally final) export instead of failing the download. **Consumers of the old draft shape (string `entry_hash`, singular `signature`, `sequence` field) must update** — the shared CoSAI-WS4 reference bundle regeneration is in flight on `claude/sleepy-northcutt-fadb4a`. (#370)
- **CoSAI-WS4 OCSF reference bundle regenerated on the final #1661 shape — and now committed to the repo.** The bundle shared with IBM on 2026-06-16 (previously untracked, main checkout only) was on the draft attestation shape; it's regenerated from a fresh production export (174 events, org chain seq 1→174, 32 agents) and lands in git for the first time at `docs/cosai-ws4-ocsf-mapping/ocsf-log-reference-bundle/`. Every event carries the final shape (`record_integrity` profile, fingerprint hashes, `attestation.uid`, per-event ECDSA signatures) — and the new `regenerate.py` proves it on every regeneration: rejects the old draft shape, verifies the full hash-chain linkage, and verifies **all 174 ECDSA-P256 signatures against the published JWKS** (public key only, no secrets), then refreshes the README's anatomy/stats blocks from the actual export so docs can't drift from data. The hand-built `signed-chain-ecdsa-example.json` is retired (superseded by real signatures in the export itself). Honest-limitations note added: the genesis row emits the literal `GENESIS` sentinel inside a fingerprint object — flagged openly as a producer nit + WG data point rather than edited out. Docs/collab artifact — no product impact.
- _Deploy:_ unbroke the GKE pipeline — the mandate image build had been failing since the ML-DSA-87 merge (#419) because the from-source liboqs build used bare `gcc` on debian-slim (can't link; needs `build-essential`). Deferred the native liboqs build entirely: it's pure cost in prod (the PQC verify path is opt-in and never imports `oqs` without a configured key), so the image build is back to the known-good recipe and the corrected liboqs build is documented in `Dockerfile.mandate` for when PQC is enabled. Internal tooling only — no product impact.
- **Case File verifier gives a friendly hint on wrong-file drops.** Dropping an evidence-anchor file (`checkpoints.json` / `inclusion-proofs.json`) or a JWKS file onto the server-side "Verify a Case File" panel previously returned "Chain broken / Signature invalid" — which reads like tampering rather than "wrong file." The panel now sniffs the JSON client-side before uploading: a real Case File report carries the audit `events[]`, so wrong files (a bare array, `{proofs}`, or `{keys}`) get a clear message that routes the user to the in-browser inclusion verifier instead, and invalid JSON is caught too. No scary crypto verdict for an obvious mis-drop, and no wasted server round-trip. (#357)
- **Evidence Anchor key directory — decision recorded (ADR-003, #407).** Reviewed per-org asymmetric key isolation now that the #406 spike landed, and decided to keep the single platform signing key with `org_id` in the signed payload: forgery resistance already lives in the per-org HMAC layer (the asymmetric key only provides public verifiability), and because every checkpoint pins its own `signer_key_id`, a future per-org-key path stays additive and verifies under one JWKS — i.e. it's a two-way door, not a deferral trap. Version-pinning and JWKS hosting were already correct and are now ratified with a rotation-safe regression test (`test_rotation_does_not_break_pre_rotation_checkpoint`). The per-org-key path is specified but unbuilt, gated on a real custody/legal/SKU trigger. No schema, endpoint, or prod-secret change. (#407)
- **Runtime agent keys now expire by default.** Newly issued runtime keys (`aid_sk_`) carry a non-null TTL (default 30 days, `RUNTIME_KEY_TTL_HOURS`) instead of living forever — a static bearer token is unsuitable even at a baseline assurance tier. Admin keys (`aid_admin_`) stay long-lived by default (operator credentials, `ADMIN_KEY_TTL_HOURS=0`). A new `POST /api/v1/agents/{id}/keys/refresh` rolls a near-expiry key to a fresh successor with the same grace window as rotation, and rotation's replacement key (previously issued without an expiry) now also carries a TTL. Existing keys are grandfathered — they keep their null expiry and stay valid — so the TTL applies only to keys issued after this change and nothing breaks on upgrade. (PR #349)
- **Monthly infra-cost report no longer recommends downgrading Neon to the Free tier.** That advice caused the 2026-06-21 production outage — the Free tier's compute-time quota is a hard wall that rejects DB connections when exhausted. The Neon flag now says keep the paid plan and treats Free as a state to fix.
- **Corrected a false regulatory claim on the finance compliance-pack.** The page stated "the SEC AI conduct rule landed in 2026" — but the SEC's predictive-data-analytics ("AI conduct") proposal was *withdrawn* in June 2025; there is no such finalized rule. Reworded to accurate framing: New York's DFS cybersecurity rules (§500.11 + AI guidance) are fully in effect, and AI-driven conflicts are a stated SEC 2026 exam priority. (Last held item from the site sweep, #337.)
- **Privacy policy sub-processor list corrected to current infrastructure.** Production moved to Google Cloud / GKE ~2 months ago, but `/privacy` still named the pre-migration stack (Render for API services) and omitted sub-processors added since. Updated Data Storage and Data Sharing to the real, config-grounded list (Google Cloud incl. KMS, Neon, MongoDB Atlas, Vercel, Stripe, Resend, Sentry, Clerk); removed Render (now internal-only); bumped "Last updated" to June 20, 2026. (#338)
- **`/architecture` page refreshed to reflect the verifiable-evidence story.** The system diagram (desktop + mobile) now frames the audit trail as OCSF event records and adds a downstream "Verifiable Evidence · Case File" node; Key Architectural Properties grew from 6 to 9 cards (added OCSF-Native Records, Independently Verifiable Evidence, Portable Case File Export); Security Layers added OCSF-Native Audit Events and Signed Forensic Attestation; compliance and CTA copy updated, with new links to `/forensics`. Independent-verifiability claims are tied strictly to the ECDSA-signed DSSE attestation (offline, public-key) — not the HMAC chain, which needs the shared key. Mandate Service and PQC deliberately omitted (pre-launch). (#336)
- **Site-wide stale-content sweep** (audit of all ~30 marketing routes). Removed pre-launch PQC/ML-DSA roadmap claims from the finance compliance-pack (incl. JSON-LD) → reframed as crypto-agility with no dates/algorithms; reframed independent-verifiability copy across home, `/forensics`, and `/vs/*` to attribute offline verification to the signed DSSE+ECDSA attestation (the HMAC chain is tamper-evident only); softened SOC 2 Type II / "courtroom-grade" / "provable in court" / "examiner-proof" overclaims to posture/"examiner-ready"; dropped HIPAA from the `/product` "live control map"; added a conservative recovery factor to the ROI calculator (was implying 100% savings); repointed 19 dead blog links (`/policies`, `/agents`, `/compliance`) to live routes; refreshed `/changelog`, stale model id (`claude-opus-4-7`→`4-8`), and a 2025 docs example date. Held for follow-up: privacy-policy sub-processor list (needs current infra confirmation) and a SEC/NYDFS regulatory-date fact-check.
- Homepage hero compliance badges reframed from certification claims to posture, under a "Compliance posture" label: SOC 2 Type II → SOC 2 Aligned, GDPR Compliant → GDPR Ready, ISO 27001 → NIST AI RMF (EU AI Act Ready kept). Labels now mirror the honest statuses on `/security` and no longer contradict the SOC 2 / ISO cert roadmap items on `/about` and `/product`. (#334)
- _CI:_ migration dry-run pins the Neon `--role-name`, fixing a repo-wide failure after a second DB role (`audit_health_ro`) was added during the Neon outage remediation. Internal tooling only — no product impact.
- _Deploy:_ the GKE pipeline now applies all four CronJobs (cleanup, compliance-export-cleanup, email, evidence-anchor) instead of leaving them to out-of-band `kubectl apply` — so a new scheduled worker reaches prod on merge. Internal tooling only — no product impact.
- _Forensics/Perf:_ added a composite index on `audit_log(org_id, id)` so per-org audit and Evidence Anchor checkpoint queries hit an index instead of scanning, and dropped internal cron/background endpoints (`/api/internal/*`) from Sentry transaction sampling to cut monitoring noise. Internal tooling only — no product impact. (#350)

### Added
- _Docs:_ **`attestation_finding` sample — a real Evidence Anchor checkpoint in the ocsf#1689 shape, with a runnable tamper demo.** New `docs/cosai-ws4-ocsf-mapping/attestation-finding-sample/` (+ shareable zip): 8 production events from the reference bundle, an RFC 6962 Merkle checkpoint built and DSSE-signed by the production code path, expressed as the proposed `attestation_finding` class (at `cffd386e`) in two variants differing by one field — references as drafted (`uid`/`type_uid`) vs references carrying `record_hash`. A standalone verifier (no repo deps) shows deletion is caught by both, while an edit verifies falsely as drafted and is pinpointed by index with hashes; `--prove N` runs an O(log N) inclusion proof. Checkpoint signed with a build-time demo key (public key inline; production uses the KMS signer). Standards/collab artifact — no product impact.
- **Mandate spend limits — monetary authority on mandates, enforced and evidenced.** A mandate can now carry a signed `spend_limit` (integer cents + ISO currency), and a new `POST /api/v1/mandates/{id}/spend` records spends against it in two modes: **enforce** (default — a spend that would cross the limit is denied and not recorded; remaining budget stays usable) and **settlement** (money already moved out-of-band is recorded even when it crosses, flipping the mandate to the new terminal **`exceeded`** state with an attached exceedance record). Every spend call — allowed or denied — is appended to the platform's hash-chained audit log via the Mandate Service's existing Postgres session, alongside new `mandate_issued`/`mandate_revoked` lifecycle entries, so *granted → spends → breach* is walkable in the OCSF export (`unmapped.mandate` block: amounts, cumulative spend, limit, deny_reason; denials read as `Denied`/severity 3) and offline-verifiable like any other chain segment. `/verify` gains a fifth check, `within_spend_limit`, which compares `spent_cents` to the limit directly — it catches an overspent mandate even if its `status` field is tampered back to `active`. **Signing fix riding along (schema 1.1):** the signable payload previously included mutable lifecycle state, so revoking a mandate broke its own signature; mandates now sign the *grant* (issuer, subject, scope, conditions, spend_limit, validity) and exclude runtime state (`status`/`revocation`/`spent_cents`/`exceedance`) — 1.0 documents keep their original verification rule byte-for-byte. Demo driver: `scripts/demo_mandate_overspend.py` (the $100-mandate → $500-spend walk). First shipped brick of the H2 agent-commerce Mandate Service.
- **Cryptographic Bill of Materials (CBOM).** A machine-readable inventory (CycloneDX 1.6) of every cryptographic algorithm, key, and implementing library across the signing, verification, integrity, and encryption surfaces — mandate signing (ECDSA P-256), the post-quantum ML-DSA-87 verify slot, Evidence Anchor checkpoints + RFC 6962 Merkle, the audit-chain and service-auth HMAC-SHA256, Clerk RS256, and Fernet credential encryption. The inventory is curated in code (`common/cbom/generator.py`) with a `file:line` citation for every claim and a drift-guard test, and aligns to the format CISA's forthcoming CBOM minimum-elements rule is being built around. Internal artifact — no product or runtime behavior change. (#420)
- **ML-DSA-87 (post-quantum) signature verify path — opt-in, verify-only.** The Mandate Service's reserved `ml-dsa-87` signature slot is now backed by a real verifier (liboqs / Open Quantum Safe), turning the crypto-agility *design* into shipped *verify* capability ahead of the Federal PQC EO timeline. **Verify only — no PQC issuance**, and **inert by default**: with no trusted PQC key configured (`forensic_mldsa_public_key`), `verify_signature` returns `None` for `ml-dsa-87` before even loading liboqs, so existing deployments are byte-for-byte unchanged and never build/load the native lib. Configuring one trusted public key (the `mldsa-local:<fingerprint>` trust scheme) enables verification of hybrid (ECDSA-P256 + ML-DSA-87) and ML-DSA-only mandates. Unlike the ECDSA path, ML-DSA signs the canonical RFC 8785 payload directly (no SHA-256 prehash). The native liboqs library is **not** built into the mandate image by default (the verify path is inert in prod, so the from-source build was deferred to keep deploys fast — re-enable per the note in `Dockerfile.mandate` when turning PQC on). Not yet enabled in any deployed environment. (#419)
- **In-browser Evidence Anchor inclusion verifier (dashboard).** The Case File page (`/forensics`) gains a public-key-only inclusion verifier: drop the `evidence-anchor/` files from a Case File bundle (`checkpoints.json` + `inclusion-proofs.json`) and the dashboard proves each event is committed to a signed Merkle checkpoint **entirely client-side** — DSSE/ECDSA-P256 signature check via WebCrypto + RFC 6962 inclusion proof — using only our published JWKS public key + SHA-256, with no server round-trip for the crypto (the lone network call fetches the public JWKS; dropping a pinned JWKS file makes it fully offline). This surfaces in the browser the same guarantee the offline CLI (`ai_identity_verify.py inclusion-proof`) gives; the verifier (`dashboard/src/lib/evidenceAnchor.ts`) is held in lockstep with the Python signer by a cross-implementation test vector generated from the real signing path. (#418)
- **Evidence Anchor — public-key verifiable Case File evidence.** A scheduled worker folds each org's audit rows into a signed Merkle checkpoint (`audit_checkpoints` table; ECDSA-P256 via the existing KMS-backed forensic signer + JWKS). Case File bundles now ship an `evidence-anchor/` folder — the signed checkpoint(s) plus a per-event inclusion proof — and the bundled verifier gains an `inclusion-proof` command, so a third party can prove any single event is committed using **only our published public key + SHA-256** (no shared secret, unlike the HMAC chain), offline. Events not yet checkpointed are listed under `pending` so coverage is explicit. Built on the #406 Merkle/checkpoint primitives. A Kubernetes CronJob runs the anchoring worker every 15 minutes. Per-org key isolation reviewed and intentionally **not** built (ADR-003, #407) — see Changed. (#406, #408)
- **Compliance export bundles now include a human-readable PDF cover letter** (`compliance_report.pdf`). Every SOC 2 / EU AI Act / NIST export already shipped machine-readable CSV/JSON evidence; it now also carries the examiner-facing "report cover letter" the export spec calls for — framework, audit period, signing key, and an inventory of the included evidence with its control mappings, plus the offline-verification statement. The PDF is written before the bundle is sealed, so its SHA-256 is enumerated in the DSSE-signed `manifest.json` like every other artifact (the cover letter is itself tamper-evident). Closes the "Machine-readable + PDF" gap on the compliance-export-profiles milestone.
- **Weekly compute-headroom report** (`scripts/infra-cost-report/compute_report.py`) — watches the compute resources prod uptime depends on (Neon, Atlas, GKE, Sentry) for *under*-provisioning and grades each CRITICAL/WATCH/OK/PROTECTED with live utilization. The inverse lens of the monthly cost report; born from the 2026-06-21 Neon Free-tier quota outage. Standing policy: err toward too much capacity on platform-essential compute — a resource is only nominated for a down-adjust after 13 consecutive weekly reports (~3mo) of sustained low use. Scheduled weekly via launchd; emailed as a branded executive PDF.
- Homepage **"Built on open standards" trust band** — surfaces the OCSF schema contributions and CoSAI WS4 membership directly below the hero, the strongest differentiated trust signal (`StandardsBadge.tsx`). Honest framing: "Contributing to OCSF" (not "merged"), CoSAI WS4 as member. (#334)
- **Case File page UX** — the filter state is now always legible and incidents are one click from investigation. A persistent summary bar shows the active window ("Showing N events · <range>") plus a removable chip per active filter and a "Clear all", so the screen is never ambiguously empty after a bad date range. The "Deny cluster detected" alert's **Investigate cluster** button now narrows the table to the cluster window (anchor ±2 min) and flips Decision→Denied before pinning the AI brief — and because both surface as clearable chips, the drill-down is reversible in one click.
- Drag-and-drop Case File verifier in the dashboard (Case File page): drop a downloaded Case File `.json` and it verifies server-side using the **same** `ai_identity_verify.py` an offline auditor runs, with your org key — a big VERIFIED / NOT VERIFIED result, no terminal, no key entry, no macOS Gatekeeper. (#332)
- Case File bundle README leads with `bash verify.command` (skips the macOS Gatekeeper block on double-clicking a downloaded script) and documents the one-time right-click → Open path. (#330)
- Case File bundle ships a double-click `verify.command` (macOS): it cd's to its own folder, auto-finds the case file, prompts only for the key, and runs both the chain and signature checks — so verifying is download → unzip → double-click → paste key, with no path, glob, or filename to type. (#328)

### Fixed
- **API: DB-bound routes no longer run on the event loop.** 34 handlers plus the `get_current_user`/`require_admin` dependencies were declared `async def` while doing synchronous SQLAlchemy (and in places synchronous email/network) work — so one slow query blocked the uvicorn event loop for *every* concurrent request instead of just its own. All are now plain `def`, which FastAPI runs in its worker threadpool; handlers that genuinely `await` (e.g. the streaming attachment download) stay async. A regression test (`api/tests/test_sync_route_execution.py`) asserts every converted callable stays sync so a future edit can't silently reintroduce the blocking pattern. No API-contract change — request/response shapes are untouched. (#402)
- **Compliance page: the HMAC chain visualization counts in `org_chain_seq` too.** Follow-up to #397 — the chain viz at the bottom of the page still labeled nodes with the global record id directly below a table counting in org chain seq. Nodes now show the seq (record id in the tooltip), so every number on the page belongs to one sequence. (#399)
- **Dashboard: a stale Clerk token no longer renders pages as empty.** Clerk session tokens live ~60s in a client-side cache; a page that sat open fired its first request burst with a just-expired token, every call 401'd, and each component's catch block rendered zeros/empty state — the Compliance page showed "0 entries / No audit entries found" while the chain-verify banner (whose request landed after the token refreshed) said 193/193, a display that reads as data loss in front of an auditor. `apiFetch` now force-refreshes the token (`getToken({skipCache: true})`) and retries exactly once when a Bearer-authenticated call returns 401; genuine auth failures still propagate. Diagnosed from pod logs + DevTools: the 401 burst at page load, the Clerk `tokens` refresh landing 200 two seconds later. (#398)
- **Compliance page: one denominator — stat cards now match the org chain for org admins.** The page mixed three scopes: the audit table and chain-verify banner were org-scoped (193 for the demo org) while the stat cards counted the caller's agents across *every* org plus shadow denials (232), and the `#` column showed the global `audit_log.id` (281) — three individually-correct, jointly-unreadable numbers for "the audit trail." `GET /audit/stats` now mirrors the list endpoint's scoping rule: org owners/admins get tenant-wide counts (reusing `_compute_stats_for_org` from the Case File path, `scope: "organization"` in the response); everyone else keeps the your-agents scope, and the Total Entries card labels which one it's showing. The table's first column now displays `org_chain_seq` (global record id in the tooltip), so dashboard rows speak the same numbering as the offline CLI — "hash mismatch at seq 180" in the terminal is row 180 on screen. `agent_id`-filtered stats keep their existing agent scope. (#397)
- **Offline verifier: agent-scoped case files no longer misreport "rows deleted".** The CLI's per-org chain mode assumed every export was an org-complete slice, so an agent-scoped case file — a legitimately *sparse* view where other agents own the intervening `org_chain_seq` numbers — failed at the first gap with "sequence gap (rows deleted from this org's history)", indistinguishable from real deletion. Found live: the overspend-walk export (seq 16–22 + 175–181) verified 7/14 then "broke." The verifier now reads the export's own `scope` declaration and mirrors the server-side rule (`verify_chain`'s agent-filter branch): per-row HMAC recomputation always runs, linkage is enforced across consecutive rows, gaps re-anchor (and are counted/reported), and the mode line says *Agent slice — completeness proof requires an org-scope export*. Org-scoped and bare exports keep the strict gap-free rule. Bundle README gains the scope note; CLI v1.2.0. Tamper detection is unchanged — an edited row still breaks at that entry.
- **Mandate audit `action_type` distinguishes limit breaches from other spend denials.** A post-breach or currency-mismatch denial was labeled `mandate_limit_exceeded`; now only a real limit crossing gets that label — other denials are `mandate_spend_denied`, with `deny_reason` carrying the detail as before. (#376)
- **Mandate `/verify` spend-limit message reports dollars, not raw cents.** The `within_spend_limit` failure string printed storage units ("spent 50000 of 10000 USD cents"); it now reads "spent 500.00 of 100.00 USD". Amounts stay integer cents in storage and in the signed grant (RFC 8785 canonicalization — floats don't canonicalize stably); only the human-readable string converts. (#376)
- **Key-epoch tracking on the audit chain — a key boundary no longer reads as tampering.** An org's `forensic_verify_key` is minted lazily (first dashboard view of the Forensics page) and can be regenerated, so one org's chain can span several HMAC key epochs — and a Case File containing rows from before the key existed could never fully verify with the customer-held key: the offline CLI reported **"CHAIN BROKEN / hash mismatch at Entry #1"**, indistinguishable from tampering (hit live on org `f3576cf6`, 0/14 verified). Fixed without rewriting a single stored hash (the append-only trigger stays honest): every audit row is now stamped at write time with a **fingerprint of the key that hashed it** (`audit_log.key_fingerprint`, SHA-256[:16], derived metadata — not part of the hashed payload); **regeneration retains the old key** in `organizations.forensic_key_history` instead of discarding it (retired keys are returned to org admins by `GET /orgs/me/forensic-verify-key`, and the regenerate response warns properly); server-side `verify_chain`/`verify_global_chain` pick the right key per row (current → retired → platform) and report intact chains with a `legacy_key_entries` count; exports carry per-event fingerprints plus a report-level `verification_key_fingerprint`; and the CLI (v1.2.0, still stdlib-only) accepts extra epoch keys via repeatable `--key`, reports rows from epochs you don't hold as **"partial key coverage"** (exit 0 — structure fully checked, coverage stated honestly) instead of CHAIN BROKEN, returns **NOT VERIFIED / no key coverage** (exit 1) when the supplied keys cover *nothing* so a typo'd key can't masquerade as a pass, and prints a **KEY MISMATCH** diagnostic when everything fails from entry #1 on a fingerprint-less legacy export. Migration `e2f3a4b5c6d7` (two additive nullable columns) + `scripts/backfill_key_fingerprints.py` stamps legacy rows (metadata-only, dry-run supported). Bundle README documents key epochs. Already-issued bundles verify exactly as before.
- **OCSF export: genesis event no longer emits the literal `GENESIS` sentinel as a fingerprint value.** The chain's first row stores `"GENESIS"` in `prev_hash`/`prev_hash_org`, and the emitter passed it through verbatim — producing a `prev_entry_hash` fingerprint whose `value` isn't a hash (`{"algorithm_id": 99, "algorithm": "HMAC-SHA-256", "value": "GENESIS"}`, seen in the production export at org chain seq 1). `select_chain` now treats the sentinel as a missing predecessor, so the genesis event simply omits `prev_entry_hash` — the same shape as a row with no chain history. Chain *verification* (CLI verifier, `common/audit`, backfill scripts) reads the raw DB columns and still uses the sentinel — unchanged. The reference-bundle validator (`regenerate.py`) keeps accepting the sentinel on seq 1 for backward compatibility with the already-shared bundle revision; the README honest-limitations note gets updated once a post-fix export is regenerated.
- **Case File verifier gives a "wrong file" hint instead of a false tamper alarm.** Dropping an `evidence-anchor/` file (`checkpoints.json` / `inclusion-proofs.json`) or a JWKS file onto the server-side "Verify a Case File" panel used to return "Chain broken / Signature invalid" — reading like tampering rather than the wrong file, a bad moment in a live court/customer demo. The panel now sniffs the JSON client-side *before* uploading (`sniffCaseFile`): a real Case File report is an object carrying the audit `events[]`/`entries[]`; a bare array is `checkpoints.json`, `{proofs}` is `inclusion-proofs.json`, `{keys}` is a JWKS file — wrong files get a friendly message routing the user to the inclusion verifier, and invalid JSON is caught too. Valid reports upload exactly as before. Frontend only. (#357)
- _Forensics:_ the Evidence Anchor checkpoint cron no longer runs `SELECT DISTINCT org_id` over the whole `audit_log` table every tick and then probes every org. It now selects only orgs whose latest `audit_log.id` is ahead of their checkpoint high-water mark — one set-based query that skips idle orgs entirely and reuses the same mark to seed each drain (`after_id`), so the `max(last_audit_id)` lookup never runs separately. Follow-up to the #346 N+1 fix; the remaining P95-latency lever on `/api/internal/evidence-anchor/checkpoint`. The run-summary field `orgs_scanned` is now `orgs_with_backlog`. Internal tooling only — no product impact.
- **Marketing-site QA sweep.** Email capture (probe pages + footer newsletter) no longer shows a false "success" on HTTP errors — invalid emails (400) and rate-limits (429) now surface an inline message, while the deliberate fail-soft on backend hiccups (202) still confirms so the probe analytics signal is preserved. Plus a11y/quality fixes: footer social icons meet the 44px tap-target minimum, the Nav "Solutions" dropdown closes on keyboard focus-out, and the React missing-key warning on all six `/vs/*` comparison pages is resolved. (#342)
- Dashboard Organization → Forensics panel: the verifier download saved as `ai-identity-verify.py` (hyphens) while the real script and the Case File bundle use `ai_identity_verify.py` (underscores), so the displayed command never matched the file. Fixed to one consistent name, and the panel was redesigned — the "shared secret, use Attestation for external auditors" caveat now sits prominently under the key, and the two redundant verify boxes are merged into one numbered CLI flow. (#327)
- Case File bundle now actually contains the verifier script. `cli/` was excluded from the Docker image (`.dockerignore`), so the bundle builder silently skipped `ai_identity_verify.py` and every downloaded bundle shipped without the tool needed to verify it. The verifier is now copied into the prod image (`Dockerfile.api`), and the bundle endpoint fails loudly (500) rather than ever shipping a verifier-less bundle again. (#326, #328)
- Case File report signature is now signed with the organization's forensic key (the one in the bundle / dashboard), not AI Identity's internal server key — so a customer's offline `ai_identity_verify.py report` shows **VALID** instead of always **INVALID**, matching the Reliability Statement's "verifiable by a key-holder" promise. Bundle README rewritten for a clean, repeatable verify. (Affects new exports.) (#325)
- Dogfood agent-identity verification works in prod again: `/api/v1/keys/verify` authenticates trusted backends (the CEO Dashboard) via a dedicated `X-Service-Token`, now mounted into the API pod from Secret Manager — completing the #298/#299 migration. (#321, #323)

### Added
- **Case File export scope** — `GET /api/v1/audit/report` (and `/report/bundle`) now export beyond a single agent + date window: by **incident** (`correlation_id` — every event sharing the id) or **org-wide** (omit `agent_id`). All four formats (JSON/CSV/OCSF/ZIP) inherit the scope, each Case File states its `scope` on its face, and org-wide / incident exports require an org owner/admin (cross-tenant is platform-admin-only). Agent-scoped exports are unchanged. (#403)
- **OCSF export** — the Case File can now export its audit events as **OCSF API Activity** (`class_uid 6003`) with the **`ai_operation` profile** and an **`attestation`** integrity object (hash-chain `entry_hash`/`prev_entry_hash`/`chain_uid`), grounded in AI Identity's OCSF contributions (PR #1641 `ai_agent` placement, #1661 provenance). New `common/ocsf` mapper + `format=ocsf` on `GET /api/v1/audit/report` (returns **NDJSON** — one OCSF event per line — the de-facto SIEM ingestion format, e.g. Splunk HEC) + an "OCSF" export button on the Case File page. The dashboard's OCSF claim is now backed by a real export (chip: "OCSF export").
- Dashboard **Attestation page** (flagship surface, under Assurance): surfaces the public **JWKS trust anchor** (`/.well-known/ai-identity-public-keys.json`), looks up and displays a **DSSE-signed session attestation** by session id (`/api/v1/sessions/{id}/attestation`) — window, event count, audit range, signer key, and the raw DSSE envelope — and explains offline verification. Enterprise-clinical, token-driven; grounded in real endpoints (no fabricated list, since attestations are retrieved per session).
- R&D strategy deliverables under `docs/strategy/`: an Agent Accountability **landscape & next-bet** report, a frontier **"Horizon 2030"** report (3–5yr; "evidence is the durable asset" thesis), and two gated design proposals — **Evidence Anchor** (transparency-log + offline verifier) and **Case File** (court-ready evidence export). Reports ship with reproducible PDF generators.
- Forensics report (**Case File**): exports now carry a plain-English **Reliability Statement** (FRE 702 / Daubert + ISO/IEC 27037 framing) describing how integrity is established, what the signature attests, the timestamp source, and the honest limits of key-holder verification. Surfaced on `/api/v1/audit/report` (JSON) and the verification bundle.
- **Case File** branding for the forensics evidence export: the verification bundle, downloaded report JSON, and CSV are now `case-file-*` (was `forensics-*`), the bundle README documents the Reliability Statement, and the API doc summaries say "Case File." Endpoint paths unchanged (non-breaking).
- Monthly **infra cost report** (`scripts/infra-cost-report/`) summarizing Neon, Atlas, GKE, and Sentry spend.
- Dashboard **design-token layer** (`dashboard/src/index.css`): semantic colors — surfaces, text, lines, brand, status — defined once per mode and exposed as Tailwind v4 utilities (`bg-surface`, `text-ink`, `border-line`, `bg-brand`…), replacing per-element `dark:` overrides. Light mode is now an intentional palette (brand uses an AA-compliant `#1d6fe0` on light, `#A6DAFF` on dark), and the app shell (layout, sidebar, theme toggle) is migrated to tokens. Phase 1 of the dashboard polish work; pages migrate in a follow-up.
- Dashboard **Overview page migrated to the design tokens** (Phase 2): stat cards, recent activity, request-volume chart, agent-health grid, quick-start, getting-started, and the system-status banner all theme through tokens — no per-element `dark:` overrides — so light mode is fully intentional on that screen. Card styling unified to flat token surfaces (the dark-mode glassmorphism/glow on stat cards was dropped for consistency).
- **Onboarding Acceptance** (dashboard QA page): the 15-step E2E checklist is now a two-party acceptance record — the customer and AI Identity each sign off — with a downloadable signed PDF bound to the run id and both signatures. (Reframed from "Simulate client onboarding.")

### Changed
- OCSF export: gateway latency is now emitted as the OCSF base `duration` field (milliseconds) instead of `unmapped.latency_ms`, per the CMF↔OCSF crossmap — reads natively to an OCSF consumer.
- Case File **exports grouped into a single "Export ▾" menu** (JSON report · CSV · OCSF · verification bundle) in the toolbar — more discoverable than the separate buttons, and surfaces the signed verification bundle (.zip + offline-verify CLI) on the page for the first time.
- Dashboard **Phase D.2 — remaining workspace migrated to design tokens**: Compliance (+ exports), Usage & billing, Shadow agents, Approvals, Web properties, Support (+ ticket detail, create-ticket modal), Admin (+ user detail, stat drawer), the Onboarding Acceptance (QA) page, the agent/key status badges, health indicator, login, and not-found. Enterprise/Pro tiers now use the `ai` accent. **The dashboard workspace is now fully token-driven** (only the deliberately-dark Demo terminal and the brand logo SVG remain non-token). Adds `success-ink`/`danger-ink`/`warning-ink` tokens and retrofits solid status buttons so their text passes AA contrast in dark mode.
- Dashboard **Phase D.1 — Agents & Keys cluster** migrated to the design tokens: Organization, Agent detail, Agents, Create agent, Agent keys, Keys pages + the agents/keys tables, cards, filters, forms, pagination, and key/agent modals. Status badges → success/warning/danger tokens; inputs/selects → tokenized; dark-mode card glassmorphism flattened. (Tier badges temporarily use `brand` pending the `ai` accent token.)
- Dashboard **Demo page** migrated to clinical tokens while keeping the bash/terminal playground deliberately dark (via the constant-navy rail tokens) — a terminal reads as a terminal in both themes.
- Dashboard **AI-feature (purple) and perf-anomaly (orange) accents tokenized** (`--ai`, `--anomaly`) with AA-accessible light + dark values, fixing low-contrast forensics accents on the light workspace.
- Dashboard **Case File workspace** (Phase C): the forensics surface is migrated off dark-only `zinc` to the design tokens (works in light + dark) and reframed as **Case File** — a court-ready evidence identity with a standards ribbon (FRE 702 / Daubert · ISO/IEC 27037 · OCSF · HMAC-SHA256), atop the existing real generate-report / chain-verify / download-bundle / verify-offline flow. Migrates `ForensicsPage` + 7 forensics components. (Distinct AI-feature purple and perf-anomaly orange accents are kept pending dedicated tokens.)
- Dashboard **shell redesigned to enterprise-clinical** (Phase A of the dashboard redesign): a constant **navy nav rail** (brand anchor in both themes) with **grouped IA** — Identity / Control / Assurance / Detect / Account — beside a **light clinical workspace** and a breadcrumb top bar. Adds `rail-*` design tokens and a shared `src/config/nav.tsx`; renames "Forensics" → "Case File" in nav. Only existing routes are linked (no dead links). Flagship surfaces (Attestation, OCSF interop) and per-page content reposition follow in later phases.
- Dashboard **Overview Phase B**: adds an **Evidence & integrity** panel wired to the real tamper-evident audit-chain verification (`/api/v1/audit/verify`) and an informational **Standards & frameworks** panel (OCSF / SOC 2 / EU AI Act / NIST AI RMF, linking to Compliance) — surfacing the trust/forensics positioning on the home screen. No fabricated metrics (attestation/Case-File counts deferred until backed by real endpoints).
- Dashboard **Overview redesigned to an action-first layout** (Phase 3): leads with a "Needs your attention" panel — pending approvals, detected shadow agents, and QA awaiting sign-off, wired to real counts (`/approvals/pending/count`, `/shadow-agents/stats`, `/qa/has-pending`) with a graceful "all caught up" state — then demotes the agent counts to a compact metric strip. Built on the design tokens. (Replaces the four stat cards / `StatsGrid`.)
- `infra-cost-report`: Treat a deliberate paid Sentry plan as a decision rather than a nag — suppresses the downgrade recommendation when the paid tier is intentional (configurable via `scripts/infra-cost-report/.env.example`).

### Fixed
- OCSF export: the `decision` → `action_id` mapping now handles long-form values (`allowed`/`denied`) and is whitespace/case-tolerant, so rows no longer fall through to `action_id: 0` ("Unknown"); `severity_id` is no longer falsely elevated for unclassified decisions.
- QA checklist runner authenticated its self-calls with the removed `X-API-Key`=email credential (broken by Insight #89), so every authenticated step failed (the "3/5" runs). It now replays the caller's Clerk session token, and onboarding runs under the caller's real account instead of a synthetic test user.

### Security
- **Removed the legacy `X-API-Key`=email authentication fallback** in the main API (`api/app/auth.py`) and the Mandate Service (`mandate/app/auth.py`). It matched the `X-API-Key` header directly against `users.email` — which is not a secret — so anyone who knew or guessed a registered user's email could authenticate as that user against the production API. Both services now require a Clerk session token (`Authorization: Bearer`); a present `X-API-Key` fails closed with a migration message. Runtime agent keys (`aid_sk_`) are unaffected — they authenticate at the gateway via the `/api/v1/keys/verify` path. Added a regression test asserting an email used as `X-API-Key` is rejected. (Insight #89)
- **`/api/v1/keys/verify` now uses a dedicated `X-Service-Token`** for service-to-service auth (constant-time compare against `VERIFY_SERVICE_TOKEN`), completing the Insight #89 migration on the consumer side — the CEO Dashboard's verify caller had been left on the removed email-as-key path and was failing closed. The general `X-API-Key` stays closed; this token is scoped to the verify endpoint only.

## [0.2.0] — 2026-04-14

### Added
- **Agent purge with retention policy** — Revoked agents are now hard-deleted after a configurable retention period (default: 30 days). Audit logs are preserved with denormalized agent names so history remains meaningful after deletion. Admin-only `POST /api/v1/admin/agents/purge` endpoint and dashboard "Purge All" button (visible when filtering to Revoked). `revoked_at` timestamp tracks when each agent was revoked.
- OpenAPI `servers` block (`https://api.ai-identity.co`) so ReDoc shows a concrete base URL.
- Tag descriptions for `capabilities`, `auth`, `audit.forensics`, `approvals`, and `shadow-agents` in the API docs.
- MIT license URL in the OpenAPI `license` block.

### Changed
- API docs `contact.url` now points to `https://ai-identity.co` (product site) instead of the repository.
- Stale "Render" references in `api/app/main.py` module and `/health` docstrings replaced with GKE / generic wording after the infrastructure migration.

## [0.1.0] — 2026-03-30

### Added
- Human-in-the-Loop approval for enterprise tier agents
- Shadow Agent Detection — customer-facing security analytics
- Automated tests for HITL approval and shadow agent detection
- Admin user detail page with agents, audit logs, and quota usage
- Request ID propagation and API versioning documentation
- Change window policy and customer notification requirements

### Changed
- Alembic migrations run automatically on API deploy
- Welcome email copy updated to warmer founder tone

### Fixed
- Approval requests migration uses IF NOT EXISTS for idempotent creation
- Performance indexes migration uses IF NOT EXISTS
- Alembic migration chain duplicate revision ID resolved

## [0.0.9] — 2026-03-24

### Added
- EU AI Act Checklist added to Solutions dropdown nav
- Solutions dropdown on landing page navigation
- Python and TypeScript SDKs with use-case blueprint pages

### Fixed
- CSP blocking Swagger UI on /docs
- Swagger docs enabled in production

## [0.0.8] — 2026-03-21

### Added
- Web Properties page and API docs link
- Redis-backed rate limiter with in-memory fallback

### Changed
- Sentry health check noise filtered, Stripe retries added, httpx connection pooling
- Gateway health check DB probe cached with 10s TTL
- uvloop + orjson added for 2-5x throughput gain

### Fixed
- P95 latency spikes — indexes, connection pooling, N+1 queries, Gunicorn tuning
- Rate limiter test accessing facade internals

## [0.0.7] — 2026-03-17

### Added
- Embedded Loom demo video and design partner callout
- Live demo link on landing page
- Why AI Identity comparison section with Okta positioning

### Changed
- Replaced remaining Framer components with custom sections
