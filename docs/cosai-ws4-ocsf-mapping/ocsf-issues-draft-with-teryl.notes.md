# Internal notes — OCSF Issues working draft

Git-only. NOT in the shared Google Doc; the `sync.sh` flow never touches this file. See scripts/cosai-docs/README.md.

### Reviewer's note — what to watch for (internal; strip before sharing)
- **Resynced 2026-06-29** from the current shared doc ("OCSF Issues — Working
  Draft", Pages export) — the repo `.md` is the source of truth again.
- **Ania's two updates applied:** Issue 2 (framework *type* already in
  `ai_agent.type_id`; only run/graph ids are genuinely new) and Issue 6
  (converge on delegation PR **#1665**, not a net-new `delegation_chain[]`).
- **#1665 currency:** sourced from Ania's 2026-06-29 message; the shared doc was
  exported at 07:45 while the live `.pages` saved at 07:46 — if a last-minute
  edit was made in Pages, re-confirm before filing.
- Don't open Issue 6 as a fresh filing — it's a refinement on #1665.
