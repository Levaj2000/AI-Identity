// @ts-nocheck
/* eslint-disable */
/* This file was generated by Unframer for Framer project 5d5a53b2b7b03258 "landio (copy)", do not edit manually */
"use client";

/**
 * @typedef Locale
 * string
 */

/**
 * @typedef {{
 * children?: React.ReactNode
 * locale?: Locale
 * style?: React.CSSProperties
 * className?: string
 * id?: string
 * ref?: any
 * width?: any
 * height?: any
 * layoutId?: string
 * from?: number // From
 * to?: number // To
 * duration?: number // Duration
 * fontFamily?: string // Font Family
 * fontSize?: number // Font Size
 * color?: string // Color
 * lineHeight?: number // Line Height
}} Props

 */
import { routes } from "./chunks/chunk-YOD5Q24P.js";

// virtual:counter
import { Fragment } from "react";
import { ContextProviders } from "unframer";

// /:https://framerusercontent.com/modules/34UYIg4nQyZcNUquRTg7/HqJnWwjW67Onm8K3BB2j/Counter.js
import { jsx as _jsx } from "react/jsx-runtime";
import {
	animate,
	motion,
	useInView,
	useMotionValue,
	useTransform,
} from "unframer";
import { useEffect, useRef } from "react";
import { addPropertyControls, ControlType } from "unframer";
function formatNumberWithCommas(number) {
	return number.toString().replace(/\B(?=(\d{3})+(?!\d))/g, ",");
}
function AnimatedCounter({
	from,
	to,
	duration,
	fontFamily,
	fontSize,
	color,
	lineHeight,
}) {
	const count = useMotionValue(from);
	const rounded = useTransform(count, (latest) => {
		return formatNumberWithCommas(Math.round(latest));
	});
	const ref = useRef(null);
	const inView = useInView(ref);
	useEffect(() => {
		if (inView) {
			animate(count, to, {
				duration,
			});
		}
	}, [count, inView, to, duration]);
	return (
		<motion.span
			ref={ref}
			style={{
				fontFamily,
				fontSize,
				color,
				lineHeight,
			}}
		>
			{rounded}
		</motion.span>
	);
}
AnimatedCounter.defaultProps = {
	from: 0,
	to: 15,
	duration: 2.5,
	fontFamily: "Arial",
	fontSize: 24,
	color: "#000000",
	lineHeight: 1.5,
};
addPropertyControls(AnimatedCounter, {
	from: {
		type: ControlType.Number,
		title: "From",
		defaultValue: 0,
		min: 0,
		step: 1,
	},
	to: {
		type: ControlType.Number,
		title: "To",
		defaultValue: 15,
		min: 0,
		step: 1,
	},
	duration: {
		type: ControlType.Number,
		title: "Duration",
		defaultValue: 2.5,
		min: 0,
		step: 0.1,
		displayStepper: true,
	},
	fontFamily: {
		type: ControlType.String,
		title: "Font Family",
		defaultValue: "Arial",
	},
	fontSize: {
		type: ControlType.Number,
		title: "Font Size",
		defaultValue: 24,
		min: 12,
		max: 72,
		step: 1,
	},
	color: {
		type: ControlType.Color,
		title: "Color",
		defaultValue: "#000000",
	},
	lineHeight: {
		// Added lineHeight property control
		type: ControlType.Number,
		title: "Line Height",
		defaultValue: 1.5,
		min: 0,
		step: 0.1,
		displayStepper: true,
	},
});
var stdin_default = AnimatedCounter;

// virtual:counter
import { WithFramerBreakpoints } from "unframer";
import { jsx } from "react/jsx-runtime";
var locales = [];
var defaultResponsiveVariants = {};
/** @type {function(Props): any} */
function ComponentWithRoot({ locale, ...rest }) {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"5d5a53b2b7b032585e2eef6b3d726ef8a269c27380483119104364b744dd4fb2"
			}
			locale={locale}
			locales={locales}
		>
			{jsx(stdin_default, {
				...rest,
			})}
		</ContextProviders>
	);
}
/**
 * @type {import("unframer").UnframerBreakpoint}
 * Represents a responsive breakpoint for unframer.
 */

/**
 * @typedef VariantsMap
 * Partial record of UnframerBreakpoint to Props.variant, with a mandatory 'base' key.
 * { [key in UnframerBreakpoint]?: Props['variant'] } & { base: Props['variant'] }
 */

/**
 * Renders CounterFramerComponent for all breakpoints with a variants map. Variant prop is inferred per breakpoint.
 * @function
 * @param {Omit<Props, 'variant'> & {variants?: VariantsMap}} props
 * @returns {any}
 */
ComponentWithRoot.Responsive = ({ locale = "", ...rest }) => {
	return (
		<ContextProviders
			routes={routes}
			framerSiteId={
				"5d5a53b2b7b032585e2eef6b3d726ef8a269c27380483119104364b744dd4fb2"
			}
			locale={locale}
			locales={locales}
		>
			<WithFramerBreakpoints
				Component={stdin_default}
				variants={defaultResponsiveVariants}
				{...rest}
			/>
		</ContextProviders>
	);
};
Object.assign(ComponentWithRoot, stdin_default);
var counter_default = ComponentWithRoot;
export { counter_default as default };
