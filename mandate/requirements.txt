# Mandate Service dependencies
# Pinned to exact versions for reproducible builds.
fastapi==0.135.1
uvicorn[standard]==0.42.0
gunicorn==23.0.0
uvloop==0.22.1
orjson==3.11.7
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

# MongoDB async driver (Atlas)
motor==3.7.0
pymongo==4.12.1

# Shared with API service — same pinned versions
sqlalchemy==2.0.48
psycopg2-binary==2.9.11

# Crypto
cryptography==46.0.5
rfc8785==0.1.4
google-cloud-kms==3.12.0
PyJWT==2.10.1
# PQC verify path (ML-DSA-87, FIPS 204) — verify only, no issuance.
# Wheel is a thin loader; the native liboqs lib is NOT built into the image by
# default (the verify path is opt-in/inert in prod and the from-source build
# broke deploys). `import oqs` is lazy and only runs when a trusted PQC key is
# configured. To enable PQC, build liboqs in Dockerfile.mandate (see note there).
liboqs-python==0.15.0

# Observability
sentry-sdk[fastapi]==2.22.0
prometheus-client==0.25.0

# Dev / testing — pytest-asyncio + mongomock-motor live in
# requirements-dev.txt (root) since they pin pytest<9 and we run pytest 9.
pytest==9.0.2
httpx==0.28.1
testcontainers[postgres]==4.7.2
