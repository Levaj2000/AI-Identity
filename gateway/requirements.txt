# AI Identity — Proxy Gateway dependencies
# Pinned to exact versions for reproducible builds.
# requirements.lock is generated by uv pip compile (command in its header); never edit the lock by hand.
# Regenerate on linux/x86_64 only (CI, Renovate, or a container): the command omits
# --python-platform because Renovate's pip-compile parser rejects that flag.
# To upgrade: update version here, test, commit.
fastapi==0.135.1
uvicorn[standard]==0.42.0
gunicorn==23.0.0
uvloop==0.22.1
orjson==3.11.7
httpx==0.28.1
sqlalchemy==2.0.48
psycopg2-binary==2.9.11
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2

cryptography==50.0.0
rfc8785==0.1.4
# Biscuit presentation-credential verify path (common/biscuit/) — pre-1.0,
# pinned hard; check wheel availability before bumping (see mandate note).
biscuit-python==0.4.0
sentry-sdk[fastapi]==2.22.0
prometheus-client==0.25.0
redis==5.2.1

# Dev / testing
pytest==9.0.2
