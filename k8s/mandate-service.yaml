apiVersion: v1
kind: Service
metadata:
  name: mandate-service
  namespace: ai-identity
  labels:
    app: mandate
spec:
  selector:
    app: mandate
  ports:
    - name: http
      port: 8003
      targetPort: 8003
  type: ClusterIP
